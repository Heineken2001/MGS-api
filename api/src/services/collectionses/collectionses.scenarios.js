export const standard = defineScenario({
    collections: {
        one: { data: { target: 'String', type: 'String', is_active: true } },
        two: { data: { target: 'String', type: 'String', is_active: true } }
    }
});
