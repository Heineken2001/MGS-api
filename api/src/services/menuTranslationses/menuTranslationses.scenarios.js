export const standard = defineScenario({
    menuTranslations: {
        one: {
            data: {
                locale: 'String',
                name: 'String',
                menus: { create: { is_active: true } }
            }
        },
        two: {
            data: {
                locale: 'String',
                name: 'String',
                menus: { create: { is_active: true } }
            }
        }
    }
});
