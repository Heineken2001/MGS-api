import ChannelTranslationsCell from 'src/components/ChannelTranslations/ChannelTranslationsCell';

const ChannelTranslationsPage = ({ id }) => {
    return <ChannelTranslationsCell id={id} />;
};

export default ChannelTranslationsPage;
